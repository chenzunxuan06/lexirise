# 词跃 LexiRise · 项目记录与问题日志

> 本文件是项目的**唯一日志入口**：记录项目内容、实施方案，以及每次修改遇到的问题（现象 / 根因 / 错误类型 / 被忽视的点 / 修复 / 验证）。
> 每次修复问题后，按「三、如何记录」的模板在本文件**追加**一条记录（最新在上），并随代码一起提交。
> 关联文档：[PROJECT.md](../PROJECT.md)（总览与规划）、[README.md](README.md)（功能与技术栈）、[doc-发音修复部署说明.md](../doc-发音修复部署说明.md)（本次发音修复的部署操作）。

---

## 一、项目内容

- **定位**：面向初中生的英语背单词 PWA 网站（PC + 手机浏览器"添加到主屏幕"即用，可离线背词）。
- **数据**：沪教牛津版 7~9 年级单词表 1535 条（1136 单词 + 399 短语），含音标/释义/例句/词根词缀；例句覆盖 568 词。
- **功能**：背书（按年级→学期→单元）· 训练 5 模式（选择/闪卡/听写/听力）· 单元测验· 复习（记忆曲线）· 错题本/生词本 · 词根词缀 · 短语专项 · 我的词表（导入）· 统计 · AI 模块（单词讲解/每日练习/单元复习包）。
- **账号体系**：用户名+密码（scrypt）+ httpOnly 会话；数据存 SQLite `web/data/user.db`；学习记录 localStorage + 自动同步。
- **技术栈**：Next.js 14（App Router，服务端模式）+ React 18 + Node 内置 `node:sqlite`（需 Node ≥ 22.5）；发音：三层引擎（见下）；部署：阿里云 ECS + 宝塔 + PM2 + Nginx（HTTPS）。

## 二、实施方案（关键设计）

### 1. 发音方案（2026-08-31 重构后）

三层自动降级，全站入口 `web/lib/tts.js`，调用方无需感知：

1. **预生成音频** `/audio/t/<sha1(规范化文本)[:16]>.mp3`——词/短语 1535 + 例句 568 去重共 **1960 个**（edge-tts 微软女声 en-US-AriaNeural 生成，约 26MB）。音色统一、离线可用（SW 缓存）。
2. **系统 TTS**（speechSynthesis）——未预生成的文本（自定义词表、中文等）。
3. **有道在线** `dict.youdao.com/dictvoice`——词/短语在线兜底（**不支持整句**，句子的接口返回 500，故整句必须靠 1 或 2）。

关键机制：
- **寻址一致性**：`lib/tts.js` 的 `speakText()` 与 `scripts/gen_audio.py` 的 `norm_text()` 必须**逐字节一致**（剥星号/括号、省略号→"something"、折叠空白），否则特殊词条（`*wrapper`、`leave ... behind` 等 102 条）的音频永远不会命中。已做全量比对（2103 条 0 差异）。
- **手势解锁**：`unlockAudio()` 在「开始训练/开始背诵」点击时调用（AudioContext + 静音占位播放）；speak 被自动播放策略拦截时挂起队列，等下一次用户手势自动重放。
- **慢速朗读**：`playbackRate=0.6`，不另外生成慢速音频。
- **音频缓存**：`sw.js` v5 加入 `/audio/t/` 缓存优先策略，播过的音频离线可重放。
- **HTTPS 前提**：浏览器端 SHA-1 寻址依赖 `crypto.subtle`（仅 secure context 可用）。

### 2. 音频生成管线

`E:\初二\scripts\gen_audio.py`：读 `words.json` → 规范化 → edge-tts 合成（并发 6、失败重试 3、**幂等**可断点续跑）→ 输出 `web/public/audio/t/`。音频文件走 Git 仓库（clone 即有），脚本可复现/补缺。

### 3. 部署与更新流程（阿里云）

- 服务器目录：`/www/wwwroot/lexirise/`（web 内容直接在此层，**没有 web/ 子目录**）。
- Node：宝塔安装的 `/www/server/nodejs/v20.18.3/bin/`（不在 PATH，命令先 `export PATH=...`）；PM2 以 **root** 运行（进程名 `lexirise`）。
- 更新流程：本机打包 zip（`pack_deploy.ps1`，排除 node_modules/.next/data）→ 宝塔文件上传 → 终端(root) `cp data/user.db data/user.db.bak-$(date +%F)` → `unzip -o` → `cp -r 解压内容/* /www/wwwroot/lexirise/`（只覆盖同名文件，不碰 data/.env.local/node_modules）→ `npm run build` → `pm2 restart lexirise && pm2 save`。
- **坑**：admin 账号对站点目录无写权限（用 root 终端或 sudo）；`sudo` 会重置 PATH（用绝对路径或 `sudo -i`）；服务器 curl 自己的公网域名会 000（阿里云 NAT 回环，正常现象，验证用手机或外网机器）。

---

## 三、如何记录（日志规范）

**每次修完问题，复制以下模板在「四、问题记录」顶部插入一条**（最新在上），随代码一起 commit：

```markdown
### YYYY-MM-DD｜一句话标题
- **现象**（用户看到的）：
- **根因**（代码/数据/平台哪一层）：
- **错误类型**：兼容性缺陷 / 数据缺陷 / 逻辑缺陷 / 部署与环境 / 性能 / 安全 / 需求变更 / 流程疏忽
- **被忽视的点**（复盘：为什么当初没发现？哪类问题容易被漏掉）：
- **修复方案**（改动要点，涉及文件）：
- **验证方式**（怎么证明修好了）：
```

**错误类型速查（含"容易被漏掉"的典型）**：

| 类型 | 典型表现 | 曾漏掉的具体例子（见记录） |
| --- | --- | --- |
| 兼容性缺陷 | API 在某平台不存在不被检测 | 鸿蒙 ArkWeb 无 speechSynthesis |
| 数据缺陷 | 音标/词条脏数据、风格不统一 | `switch-off` 音标 `''`；5 处音标与读音不符 |
| 逻辑缺陷 | 判分/比较规则与展示不一致 | 听写判分对连字符/括号/省略号词条必判错 |
| 部署与环境 | 服务器环境与本地差异 | sudo 重置 PATH、admin 无写权限、NAT 回环 000 |
| 流程疏忽 | 生成/寻址两端规则不一致 | 音频 hash 的规范化两端不一致 |

---

## 四、问题记录

### 历史问题速览（2026-08-31 之前，依据本仓库 git 提交记录与 `.workbuddy/memory` 开发记忆整理）

| 日期 | 问题 | 错误类型 | 修复要点 | 教训点 |
| --- | --- | --- | --- | --- |
| 08-14 | 词库数据源错误：此前清洗/例句/音标建立在错误数据源上，权威源改为 6 份教材 PDF 词表 | 数据缺陷 | 按 PDF 重导数据（import_hu_jiao.py） | 数据型功能上线前必须核对数据源权威性 |
| 08-14 | Python 用 `/e/初二/...` 路径写文件**落到错误根目录**（"导出成功"却读不到） | 环境缺陷 | 必须用 Windows 盘符路径 `E:/初二/...`，写完 `os.path.exists` 验证 | 跨 shell 路径语义差异；写盘必须验证存在性 |
| 08-14 | 例句中文翻译全面失败：huggingface 被沙箱封锁、Google 超时、MyMemory 429 限流 | 外部依赖 | 结论：自动化机翻不可行，改人工/LLM 翻译批次（examples_zh_1~4.json） | 第三方翻译服务在受限网络不可依赖，预留人工通道 |
| 08-14 | `next dev`/`next build` 清理 `.next` 时被 safe-delete 垫片 fail-closed 拦截而崩溃 | 环境缺陷 | 打补丁 `node_modules/next/dist/lib/recursive-delete.js`（**重装 node_modules 需重打**） | 沙箱拦截器会改变构建工具行为；补丁记录要留档 |
| 08-15 | 本机沙箱 curl 返回空体（HTTP 不通假象） | 环境缺陷 | 用 Python urllib 验证 | 验证工具本身可能不可信，换工具交叉验证 |
| 08-15 | `backup_daily.ps1` 中文注释解析错 | 环境缺陷 | ps1 必须存为 **UTF-8 BOM**（PowerShell 5.1 无 BOM 按 ANSI 读） | 本次部署打包脚本（pack_deploy.ps1）中文路径乱码**同款再犯**，已加固 BOM |
| 08-15 | Node 18/20 不支持 `node:sqlite`，CloudStudio 部署报错 | 兼容性缺陷 | 双后端适配器（node:sqlite / better-sqlite3 按版本自动选）+ API 全异步 | 运行环境版本差异必须在部署文档中标注并主动适配 |
| 08-15 | 用户误贴 GitHub 令牌（gho_）到聊天 | 安全事件 | 提醒立即撤销令牌 | 凭据类内容一律打码后再贴日志 |
| 08-20 | `/api/ai/status` 被 Next 静态预渲染：构建时无 key 则线上永远"未配置" | 逻辑缺陷(SSG) | 强制 `force-dynamic` | 服务端 API 路由要防被预渲染（读构建期环境变量的 API 尤其） |
| 08-28 | 备份/管理脚本在 Node 18/20 下 ESM 导入报错 | 兼容性缺陷 | `_backup.mjs` 改自包含实现 | ESM/CJS 判定不要依赖扩展名假设 |
| 08-28 | AI 练习中 `*` 标记词条（超纲词）答案判定错误 | 数据缺陷 | 答案判定剥离 `*` 前缀（v3） | **`*` 星号词条是个反复出现的坑**：本次发音音频寻址又遇到（speakText 剥星号）。词库中 71 个 `*` 前缀词条，任何按文本比较/寻址的逻辑都要统一剥离规则 |
| 08-28 | 手机横屏时侧边栏划不动 | 兼容性缺陷(移动端) | 侧边栏改抽屉+汉堡按钮+遮罩 | 移动端适配要覆盖横屏/手势层 |
| 08-15~ | `data/user.db` 提交风险、SQLite 多进程锁（database is locked）、重启后服务停、Vercel 需换托管库、SW 缓存版本需手动 +1 | 流程疏忽 | 已列入 PROJECT.md「已知问题」1~9 | 部署类坑集中在 PROJECT.md，改造/新增部署路径时先读 |

### 2026-08-31｜鸿蒙/手机端发音全部无声（听力、听写、所有 🔊 按钮）
- **现象**：鸿蒙手机（ArkWeb 内核浏览器）打开听力模式：进入题目后没有任何声音，无报错、无提示；iPhone 等其他端正常。用户另反馈"有个别词音标不对，一个单词被读成两个词"。
- **根因**：全站发音 100% 依赖浏览器 Web Speech API（`speechSynthesis`）。`lib/tts.js` 原实现检测到 `"speechSynthesis" in window` 为 false 时**直接静默 return**——鸿蒙 ArkWeb 未实现该 API，导致全站朗读在鸿蒙上静默失效，且没有任何降级与提示。
- **错误类型**：兼容性缺陷（单点依赖无降级）+ 数据缺陷（音标脏数据）+ 逻辑缺陷（听写判分）+ 流程疏忽（多处）。
- **被忽视的点**（重点复盘）：
  1. **单点依赖无降级**：52 处调用全部走一个函数、无音频文件后备——"某个平台没这 API"的场景完全没考虑。教训：浏览器 API 使用前必须做能力探测并留降级路径，涉及"发音/播放"的功能尤其要在移动端多内核验证。
  2. **听写判分与朗读文本不一致**：判分用 `word_en` 原文严格全等，而朗读文本对 `switch-off`/`(be) busy with`/`leave ... behind` 类词条做了规范化——学生"听对了也必然判错"。这类"展示/判分与发音规则不同步"的隐蔽 bug 长期存在没人发现。教训：凡是"听音作答"的功能，判分规则必须与发音规则同源。
  3. **生成端与浏览器端 hash 规则不一致**：音频生成脚本对词条做了规范化（剥星号/括号），浏览器端却用原文寻址，导致 102 个特殊词条音频永不命中。教训：两侧共用的寻址/规范化规则必须写死并做全量比对（本次 2103 条 0 差异验证）。
  4. **`next start` 的 public 静态文件映射是启动时建立的**：服务运行中新增的文件 404——部署时文件必须齐全后再启动/重启。
  5. **检测页测试词必须来自词库本身**：初期用 "apple" 检测，但初中沪教版词库没有该词（小学词），会造成误报；改用词库必有的 "guitar"。
  6. **音标数据从无人核查**：`switch-off` 的 `/swɪtʃ''ɒf/`（拼接脏字符）、`semi-final` 空音标、`enter`/`glove`/`compare`/`international`/`focus` 五处音标与标准读音不符（用有道词典 API 逐一核验）。教训：数据类资源要建立周期性核对机制（本次用有道接口抽样核验，覆盖率 20/20）。
  7. **服务器环境差异**：admin 无写权限（Permission denied）、`sudo` 重置 PATH（`sudo: node: command not found`）、服务器 curl 自己公网域名 000（NAT 回环，非故障）。教训：部署验证用外网视角（本机/手机），不要用服务器 curl 自己。
  8. **英/美音标混用**：重复词条跨册音标不一致（`survey` `/sɜːveɪ/` vs `/səˈveɪ/` 等），149 条旧式撇号写法（`'saɪlənt`）——不影响发音但与统一美音发音显示观感不一致，**待决策是否统一**。
- **修复方案**：
  1. `web/lib/tts.js` 重构为三层发音引擎（预生成音频 → 系统 TTS → 有道在线），失败自动降级、不再静默。
  2. `scripts/gen_audio.py` 新增，edge-tts 生成 1960 个发音文件（词/短语+例句，幂等可重跑）。
  3. `web/lib/tts.js` + `scripts/gen_audio.py` 规范化规则逐字节对齐（sha1[:16] 寻址），全量比对 0 差异。
  4. `web/app/train/page.jsx`、`web/app/recite/page.jsx`：开始训练/背诵时 `unlockAudio()` 手势解锁，防移动端自动播放拦截。
  5. 听写判分改为规范化键比较（连字符=空格、剥括号/星号、省略号→something，`normKey()`）。
  6. `web/public/sw.js` v5：新增 `/audio/t/` 缓存优先（离线可重放），预缓存诊断页。
  7. `web/public/words.json`：修复 7 处音标（switch-off、semi-final、enter、glove、compare、international、focus）。
  8. `web/public/tts-diag.html`：新增手机端 5 项发音检测页（API 存在性/引擎出声/在线音频/本站音频包/结论）。
- **验证方式**：
  - 本机：2103 条两端 hash 全量比对 0 差异；headless（无语音引擎≈鸿蒙形态）实测预生成命中/404→有道降级/星号剥除/中文不误降级；HTTP 层音频 200 `audio/mpeg`；三次 `npm run build` 通过。
  - 服务器：外部视角抓包确认新版 JS（含 audio/t 寻址与有道兜底）已上线、音频文件字节数与本地一致。
  - 真机（用户）：鸿蒙 + iPhone 跑 `tts-diag.html` 5 项检测全过，听力模式出声正常 ✅。

---

## 五、容易忽略的坑清单（长期维护，新坑随时加）

1. 浏览器 API 使用前做能力探测 + 预留降级（speechSynthesis / crypto.subtle / AudioContext 都中过招）。
2. 听音作答类功能：判分规则与发音规则必须同源（规范化一致）。
3. 两端共用的 hash/规范化规则：改了任何一侧必须全量比对另一侧（本仓库脚本 + tts.js 之间）。
4. 部署 zip 排除项：node_modules / .next / data(数据库!) / 本地临时文件；上传后先 `wc -l` 核对 audio 文件数（1960）。
5. 服务器操作：root 或 sudo；sudo 下 PATH 会被重置（用绝对路径）；先备份 `user.db` 再覆盖。
6. 阿里云机器 curl 自己公网域名返回 000 = NAT 回环正常现象，验证请用外部视角。
7. `next start` 静态文件映射启动时建立：新文件必须重启服务后才可访问。
8. 检测/测试用的词必须从词库实际数据中选（apple 在初中词库不存在）。
9. 修改 `sw.js` 缓存结构后必须 `CACHE` 版本号 +1，否则旧缓存不更新（PROJECT.md 已知问题 8）。
10. 服务器 node_modules 与 lock 文件需保持同步：构建报错先 `npm config set registry https://registry.npmmirror.com && npm install`。